apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: mean-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mean-backend
  template:
    metadata:
      labels:
        app: mean-backend
    spec:
      containers:
      - name: backend
        
        image: jaishankar7655/mean-backend:latest
        ports:
        - containerPort: 8080
        env:
        - name: MONGO_URL
          
          value: "mongodb://mongo:27017/dd_db"
---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: mean-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
